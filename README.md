fullstack-lab

Frontend: frontend/ (React)
Backend: backend/ (Spring Boot starter)

Run frontend:
cd frontend
npm install
npm start

Run backend later (when implemented)
cd backend
mvn spring-boot:run

